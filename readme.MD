# Step 1 install library satu kali lewat terminal

pip install nltk contractions gradio groq sentence-transformers rank-bm25 scikit-learn pandas matplotlib seaborn

# Step 2 buat file .env dan tambahkan Groq API KEY (https://console.groq.com/keys)
GROQ_API_KEY= .....

jangan lupa pip install python-dotenv
